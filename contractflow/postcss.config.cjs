module.exports = {
    plugins: {
        // Tailwind v4 moved the PostCSS plugin into a separate package
        // named '@tailwindcss/postcss'. Install it with:
        // npm install -D @tailwindcss/postcss
        '@tailwindcss/postcss': {},
        autoprefixer: {},
    },
};
